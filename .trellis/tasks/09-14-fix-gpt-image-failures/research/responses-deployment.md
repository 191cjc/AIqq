# Responses 图片通道部署结果（2026-09-15）

## 已交付行为

- 生产装配改为 `CodexResponsesImageService`，使用真实 Codex Python SDK/CLI 驱动 `gpt-6-astra`，通过 `/responses` 的 `image_generation` 工具生成和编辑图片。
- `OPENAI_IMAGE_DRIVER_MODEL` 默认 `gpt-6-astra`；工具模型 `OPENAI_IMAGE_MODEL` 默认 `gpt-image-2`。保留独立图片密钥/地址；显式空值按文档回退聊天配置。
- 每个业务尝试使用独立临时令牌和回环监听器，SDK 不持有真实图片密钥；只接受固定路径、驱动模型和一轮 SDK 请求。上游保留真实 SDK 请求头。
- 仅 HTTP 502 在开始流之前最多重试两次，间隔 0.5/1 秒。其他状态、连接异常、超时、流中断和无效图片不重试。SDK 自动请求/流重试关闭。
- 成功以完整 `response.completed` 和已完成图片结果为依据；图片规范化为 1024×1024 JPEG。超时、取消及服务关闭清理 SDK、临时目录、本机监听器和上游请求。
- 原有审核、共享额度与 QQ 图片发送入口复用此适配器；普通聊天的原生图片工具仍关闭，群历史配置仍为 10000 字符。

## 代码验证

- 完整 `tests/unit`：160 项通过（5.154 秒）。最后补充图片空配置回退修正后，5 项配置测试单独通过，其中包含新增回归。
- 图片适配器 16 项、SDK 12 项定向测试通过。独立复核额外验证 3 项边界回归；没有剩余阻塞项。
- 复核修正压缩 HTTP 502 正文重复解压导致跳过重试的问题，并覆盖异常 SSE 类型、非 ASCII 鉴权头、固定路径/查询参数及已接收但未读完的请求取消清理。
- `git diff --check`、改动 Python 文件语法检查通过；项目未配置独立 lint/type-check 命令。

## 正式适配器真实验证

2026-09-15 15:23:46 开始，仅执行生成一次、成功后编辑一次，两次均使用当前图片密钥；没有代发 QQ 消息。

| 操作 | 耗时 | HTTP | 返回文件 | 上游 request ID |
| --- | --- | --- | --- | --- |
| 文生图 | 28.549 秒 | 200 | 1024×1024 JPEG，18531 字节 | `8ffc0e1c-b902-48d3-8093-47f55b7a6268` |
| 参考图编辑 | 30.993 秒 | 200 | 1024×1024 JPEG，18442 字节 | `b49ffdda-e6b6-4921-ac67-381779bdee47` |

两次上游请求 `originator=codex_sdk_py`，均未使用 Responses Lite。图像解码及人工目视检查通过：蓝色圆形编辑为绿色，白色背景和构图保持。临时运行目录清理完成。

实测证据及图片：`/var/lib/aiqq/backups/astra-production-adapter-check-20260915T152346/`。

## 部署与回滚

- 2026-09-15 15:25:22 +08:00 重启 `aiqq.service`，新 PID `898856`，active/running，NRestarts=0。
- 15:25:24 QQ 连接就绪。本地、公网健康接口 HTTP 200；QQ connected=true，数据库打开。
- 依据运行进程初始环境和其 `.env` 合并规则核对：图片驱动 `gpt-6-astra`，工具模型 `gpt-image-2`，群历史 10000 字符；运行服务图片密钥和地址与实测完全一致（记录仅保存比较布尔值）。
- 精确备份、此次补丁、发布文件 SHA256、测试日志、健康及运行配置记录：`/var/lib/aiqq/backups/gpt-responses-migration-20260915T150704/`。没有备份或输出凭据，没有修改 `.env`。
- 回滚仅恢复该备份 manifest 中列明的旧文件、移除其中列明的两个新增文件，再重启；不得 reset/clean 整个未提交工作区。回滚会回到旧 Images API 路线，该路线最近实测仍有 404，不是可用性保证。

## 尚待用户群内验收

服务层真实生成和编辑已恢复；QQ 网关连接及既有交付路径已验证到代码/离线测试层面。没有发送测试 QQ 消息，用户可主动发起 `/GPT生图` 或引用改图完成可见交付验收。任务保持 in_progress，保留原始 QQ 交付验收项；不把本次结果描述为已在群中发送成功。
