# 执行方案 v4 — 实现与部署完成

用户于2026-09-16明确“开始改动”，随后要求“继续工作”。当前执行已确认的 v4：最近50条全文与完整字段、更早历史和图片按需读取；不持久保存图片原文件。v1缓存方案已废弃。

## 实施状态

- [x] 读取规范，更新实施/审查上下文，保存精确源码和SQLite一致性基线：`/var/lib/aiqq/backups/chat-read-20260916T152109/`。
- [x] 群级消息身份、追加原事件/处理状态/消息版本/附件链接元数据/发送尝试表及legacy迁移。
- [x] 网关先保存原文再投影，缺字段保留已有数据；失败投影可恢复且不重放业务。原始事件持久化后异步处理对话，避免阻塞后续接收。
- [x] 同群全行映射与最新50条/当前请求统一输入，保留新列、未知嵌套、空值、换行、进度和撤回状态；不再执行旧字符预算。
- [x] 同群历史/版本/原事件分页及筛选，已知消息的unsupported事件可追溯；没有真实平台支持的成员撤回/修改事件不臆造。
- [x] aiqq-chat-read技能、历史与图片脚本、服务端群绑定、请求预算及完整历史直接模型输入，绕过命令输出截断。
- [x] Codex聊天独立脚本/view_image权限，Landlock+seccomp文件与进程隔离、短期令牌及服务端保管模型密钥；备用Responses有界工具调用。
- [x] 静态图片/动图有界解码、最多3来源9帧，临时文件结束/失败/取消及异常残留清理，不预取、不持久缓存原图。
- [x] 原图/压缩交付图实际尺寸、大小、MIME、哈希及来源元数据；发送失败不产生成功消息。保留GPT/NovelAI、600秒前台等待、2MiB压缩和纯图片补发。
- [x] 全部识图读取失败时，将确定的原因同步到全文与群内摘要；混合失败保留各自原因。
- [x] 生产备份副本803条迁移：原列/记录号逐项相同，803个legacy快照，重复初始化幂等；部署时实际808条业务记录迁移并逐项核对通过。
- [x] 一次真实识图验收：gpt-6-astra经完整ChatAgent/记录/脚本/view_image识别两张卡片的像素文字，76.36秒，无生图或QQ群发送，临时目录清空。
- [x] 最终独立审查无阻塞问题；完整264项测试、75项最终定向测试、编译、技能及diff校验通过。
- [x] 服务空闲时更新一致性备份；16:09:51重启，PID1297532，本地/公网HTTP200，QQ已连接，NovelAI就绪；808条旧记录所有原列/编号逐项一致。

## 验收与记录

固定契约见prd.md的V1–V17、design.md、storage-design.md及`.trellis/spec/backend/chat-read.md`。
关键回归涵盖全50条大于10000字符、未知列/字段/23+附件、同群分页/版本、当前纯图片@、原包先提交与失败恢复、真实CLI脚本+多图像素传输、30000字符历史与未知尾字段、不允许读取宿主数据库或其他回合、临时子进程清理、错误原因不被摘要隐藏，以及现有生图/QQ交付回归。

完整测试命令：`.venv/bin/python -m unittest discover -s tests/unit -q`。
项目没有单独声明lint/type-check工具；运行compileall和git diff --check，不把语法编译声称为类型检查。
最终结果汇总在execution.md、review.md、runtime-result.md、migration-probe.json及live-vision-result.json。

## 回滚约束

迁移将旧的全局message_id唯一性改为同群复合身份。回滚必须保留兼容的群级仓库写入器与新增表、部署后新消息，不能直接恢复旧的ON CONFLICT(message_id)代码，也不能用旧数据库覆盖新收到的记录。源码/数据库备份用于诊断和受控恢复，不代表允许丢弃新增数据。
