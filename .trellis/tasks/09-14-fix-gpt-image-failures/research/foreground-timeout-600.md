# 前台等待延长至 600 秒（2026-09-15）

用户明确选择“前台等待延长到 600 秒”，并要求放宽长任务判定，让刚才约 6 分钟的图片任务继续按正常流程等待。此前已说明项目使用的 QQ 5 分钟被动回复窗口与本地等待阈值是独立限制；用户仍选择 600 秒，不应擅自恢复 280 秒。

- 原有效前台阈值为 `min(OPENAI_TIMEOUT_SECONDS=280, AIQQ_AI_TOTAL_TIMEOUT_SECONDS=290)`，即 280 秒。
- 在运行 `.env` 中显式设置两项为 600，实际前台等待阈值及 SDK 慢回合标记均为 600 秒。必须同时设置，避免较小值继续提前触发后台分支。
- `OPENAI_IMAGE_TIMEOUT_SECONDS` 仍为 240 秒；群历史仍为 10000 字符。本轮无源代码修改、无新生图调用、无 QQ 代发。
- 2026-09-15 17:05:20 +08:00 重启服务，新 PID 927633，active/running，NRestarts=0。核对进程环境与 `.env` 合并后的有效阈值为 600 秒，本地/公网健康 HTTP 200、QQ connected=true、数据库打开。
- 仅备份非敏感的两项原配置状态及验证记录：`/var/lib/aiqq/backups/foreground-timeout-20260915T170520/`。原两项均未显式配置，回滚时移除它们即可恢复默认值。

这项调整放宽长任务判定，不改变 QQ 平台本身的回复时限，也没有修复超过 600 秒后后台结果只保存文字的已有缺陷；此前已完成但未保存的成品不会因修改阈值恢复。
